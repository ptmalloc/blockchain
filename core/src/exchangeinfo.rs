use crate::netrpc::*;

/*暂时放弃
pub fn a() {

    let b_length = connect_recv_legnth();
    let a_length = get_local_length();
    if (a_length > b_length){
        connect_send_length();
    }

}

pub fn b(){
    let a = connect_recv_block();
    connect_recv_length();
}*/
